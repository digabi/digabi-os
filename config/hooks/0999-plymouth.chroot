#!/bin/sh

set -e

# Set Plymouth theme
THEME="mytheme"

if [ -e "/usr/sbin/plymouth-set-default-theme" ] && [ -e "/usr/share/plymouth/themes/${THEME}/${THEME}.plymouth" ]
then
    /usr/sbin/plymouth-set-default-theme "$THEME"
fi
