#!/bin/sh

set -e

case "$(dpkg --print-architecture)" in
    amd64)
        apt-get -y install libsndio7.0:i386=1.5.0-3 wine32:i386
    ;;
    *)
        echo "W: Unsupported architecture, not installing multiarch packages."
    ;;
esac
