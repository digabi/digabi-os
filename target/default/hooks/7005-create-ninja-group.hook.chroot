#!/bin/sh
if [ -n "${DIGABI_DEBUG}" ] ; then set -x  ; fi

set -e

addgroup() {
    GID="$1"
    GROUP="$2"
    /usr/sbin/groupadd --gid ${GID} ${GROUP}
    echo "I: Added group ${GROUP} with GID ${GID}."
}

addgroup 2200 ninja
