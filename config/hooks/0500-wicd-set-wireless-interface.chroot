#!/bin/sh

set -e

WICD_CONFIG="/etc/wicd/manager-settings.conf"

if [ -f "${WICD_CONFIG}" ]
then
	sed -i 's/wireless_interface = None/wireless_interface = wlan0/' "${WICD_CONFIG}"
else
	echo "I: wicd config not found - not configuring wlan0"
fi
