#!/bin/sh

set -e

# Set Plymouth theme
THEME="spinfinity"

if [ -x "/usr/sbin/plymouth-set-default-theme" ]
then
    if [ -f "/usr/share/plymouth/themes/${THEME}/${THEME}.plymouth" ]
    then
        /usr/sbin/plymouth-set-default-theme "$THEME"
        /usr/sbin/update-initramfs -u
    fi
fi
